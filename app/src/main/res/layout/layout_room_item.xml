<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="92px"
    android:id="@+id/rl_item"
    >
  <!--  android:background="@drawable/bg_room_select"-->




    <ImageView
        android:id="@+id/imv_scene_pic"
        android:layout_width="80px"
        android:layout_height="80px"
        android:layout_gravity="center"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_home_commonly_used_default"
        android:paddingLeft="40px"
        />

  <RelativeLayout
      android:id="@+id/rl_edit_item"
      android:layout_width="match_parent"
      android:layout_height="match_parent"
      android:layout_marginTop="12px"
      android:layout_marginBottom="12px"
      android:layout_marginLeft="12px"
      android:layout_marginRight="36px"
      android:background="@color/layou_menu_edit_bg_defult"
      android:layout_toRightOf="@+id/imv_scene_pic"
      >

    <TextView
        android:id="@+id/tv_scene_name"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="房"
        android:maxLines="1"
        android:gravity="center_vertical"
        android:ellipsize="end"
        android:textColor="@color/com_white"
        android:textSize="32px"
        android:layout_centerVertical="true"
        android:layout_marginLeft="22px"
        android:layout_toLeftOf="@+id/iv_delete"

        />
  </RelativeLayout>


</RelativeLayout>